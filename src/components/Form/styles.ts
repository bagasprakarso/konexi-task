import styled from "@emotion/styled";

/**
 * A styled div component that serves as a wrapper for forms.
 * It uses flexbox to arrange its children in a column direction with a gap of 12px between them.
 */
export const FormWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 12px;
`;
